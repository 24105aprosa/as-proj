$TTL 86400
@   IN  SOA dns.{domain}. admin.{domain}. (
        {serial} ; serial
        10800 ; refresh
        3600 ; retry
        604800 ; expire
        86400 ; minimum
        )
@   IN  NS  dns.{domain}.
@   IN  PTR   {fqdn}